// Tabuada 7

let multiplicador = 1


while (multiplicador <= 10) {
    let i = 1
    while (i <= 10) {
        console.log(`${multiplicador} X ${i} = ${multiplicador * i}`)
        i++

    }
    console.log(" ")
    multiplicador++
}