let text = "Estão chegando as provas"

// Tamanho da string
console.log(text.length)

//Replace
console.log(text.replace("provas", "avaliações"))

// Transformado o resultado em MAIUSCULO
console.log(text.toUpperCase())

// Transformando o resultado em minusculo
console.log(text.toLowerCase())

// Retorna a primeira posição na string
console.log(text.indexOf("as"))

// Retorna a última posição na string
console.log(text.lastIndexOf("as"))
